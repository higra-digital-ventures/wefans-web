// Fade-in suave a cada navegação (re-monta por rota) — transição do Top Shot.
export default function Template({ children }: { children: React.ReactNode }) {
  return <div className="wf-page">{children}</div>;
}
